{% extends 'base.html.twig' %}

{% block title %}Créer une séance{% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="mb-4">Créer une nouvelle séance</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success mt-3">Enregistrer</button>
        {{ form_end(form) }}

        <a href="{{ path('app_seance') }}" class="btn btn-link mt-3">Retour à la liste</a>
    </div>
{% endblock %}
